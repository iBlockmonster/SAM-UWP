<UserControl
    x:Class="SAM.DataTemplates.HeaderUserControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SAM.DataTemplates"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid x:Name="LayoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="122*"/>
            <ColumnDefinition Width="175*"/>
            <ColumnDefinition Width="103*"/>
        </Grid.ColumnDefinitions>
        <Grid x:Name="ClockLayoutGrid" HorizontalAlignment="Center" Height="100" VerticalAlignment="Center" Width="100">
            <Grid.Background>
                <AcrylicBrush FallbackColor="#33000000" TintColor="#33FFFFFF"/>
            </Grid.Background>
        </Grid>
        <Grid x:Name="LogoLayoutGrid" HorizontalAlignment="Center" Height="100" VerticalAlignment="Center" Width="100" Margin="0,0,0,0" Grid.Column="1"/>
        <Grid x:Name="WeatherLayoutGrid" HorizontalAlignment="Center" Height="100" VerticalAlignment="Center" Width="100" Margin="0,0,0,0" Grid.Column="2"/>

    </Grid>
</UserControl>
